1. In root project directory open console and run "mvn clean install"
2. run docker compose (docker-compose -f docker-compose.yml up)
3. run Application

Requrements:
Java 1.8
Docker or MySQL.
Maven (bundeled in IntelijIDEA)